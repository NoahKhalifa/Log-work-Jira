# Report — 2026-05-28

## Phiên 17:47

### Yêu cầu
Bỏ fix cứng các hằng số log-work (`USERNAME`, `TIME_SPEND`, `REMAINING_TIME`, `DESCRIPTION`) — đưa lên UI cho người dùng nhập với giá trị mặc định. Đồng thời nghiên cứu: (a) có thể lấy `username` (user key) tự động từ tài khoản login không, (b) có thể lấy danh sách option của `fieldName`/`fieldValue` từ Jira không.

### Thay đổi
- `app.js:32-40` — đổi các hằng cứng thành `DEFAULT_*` (chỉ dùng khi UI không gửi). Đặc biệt `DEFAULT_TIME_SPEND_HOURS = 8` (UI dùng đơn vị giờ).
- `app.js:497-516` — thêm hàm `fetchUserKey()` gọi `GET /rest/api/2/myself` để lấy `key` (vd `JIRAUSER14218`). `loginToJira()` gọi xen vào và trả thêm `userKey`; lỗi của bước này không làm fail login.
- `app.js:560-573` — `/api/session` trả thêm `userKey` để UI autofill.
- `app.js:599-612` — `/api/login` lưu + trả `userKey`.
- `app.js:78-83` — thêm CSS `.settings-box`, `.settings-row`, `.field-hint` cho khối "Log Work Settings".
- `app.js:133-166` — section "Log Work Settings" mới (User Key, Time Spend giờ, Remaining giờ, Description, Field Name, Field Value) đặt ngay trên nút Execute.
- `app.js:268-274` & `app.js:286-291` — `loadSavedSession()` và `login()` autofill `#userKey` từ session/response.
- `app.js:359-405` — `execute()` thu thập + validate 6 trường mới, tách `fieldValue` theo dấu phẩy, gửi đầy đủ qua `/api/execute`.
- `app.js:715-746` — `/api/execute` đọc params, fallback về DEFAULT khi thiếu, convert giờ→giây (`*3600`), dựng `logWorkConfig` rồi truyền vào `runLogWork`.
- `app.js:760` + `app.js:813-823` — `runLogWork` nhận `logWorkConfig` và dùng các giá trị động cho payload `create-log-work` thay cho hằng số cũ.

### Quyết định & lý do
- **USER_KEY autofetch qua `/rest/api/2/myself`**: là endpoint Jira chuẩn, trả về `key` đúng định dạng `JIRAUSER<id>` mà payload `create-log-work` cần. Lỗi của bước này nuốt im lặng để không làm vỡ flow login — UI vẫn cho user nhập tay nếu cần.
- **Time Spend nhập theo giờ thay vì giây**: thân thiện hơn (default `8`), backend tự `*3600`. Đã loại bỏ option "cú pháp Jira 8h 30m" vì không cần thiết và phải viết parser.
- **`fieldValue` nhập text rồi split theo dấu phẩy**: đơn giản, support cả single và multi value mà không cần UI phức tạp. Mặc định `"Type Of Work"` / `"Test"` giữ nguyên hành vi cũ.
- **Field options CHƯA tự fetch**: `f-timesheet` là plugin nội bộ Viettel, không biết endpoint config field nào để gọi. Giải pháp tạm: input free-text. User sẽ chụp endpoint từ DevTools (Network → mở popup Log Work trên Jira) để phiên sau gắn dropdown.
- **Section đặt mở sẵn, ngay trên nút Execute**: 4 giá trị này thay đổi tương đối thường xuyên (đặc biệt Time Spend ở các issue có giờ lẻ), nên ưu tiên hiển thị thay vì giấu trong `<details>`.
- **Cấu hình lưu vào session**: chỉ lưu `userKey` (vì lấy được từ Jira). `timeSpendHours`/`description`/v.v. KHÔNG cache giữa các phiên để tránh nhầm — user phải confirm mỗi lần.

### Verify
- [x] `node --check app.js` — pass, không có lỗi cú pháp.
- [x] `PORT=3099 node app.js` chạy được; `GET /` trả HTML có đủ 6 input mới (`#userKey`, `#timeSpendHours`, `#remainingHours`, `#description`, `#fieldName`, `#fieldValue`) và tiêu đề "Log Work Settings".
- [ ] **CHƯA verify được trên Jira thật**: không có credential Jira trong môi trường này. Cần chạy `node app.js` ở máy user, login → kiểm tra `userKey` có autofill đúng `JIRAUSER14218`, thực thi 1 lượt JQL test (ví dụ giới hạn 1 issue) và confirm worklog tạo đúng `timeSpent`, `description`, `Type Of Work`.

### Việc còn dang dở / cần làm tiếp
- **Nhận endpoint f-timesheet config từ user** → đổi `#fieldName` thành `<select>` + `#fieldValue` đa-select sau khi biết URL/response shape.
- (Tuỳ chọn) Lưu lần dùng cuối của `timeSpendHours` / `fieldValue` vào `.jira-session.json` để autofill lại — chưa làm vì không chắc user muốn.
- Cập nhật `update Logwork Jira.js` cho đồng bộ — theo CLAUDE.md mục 2, **không tự sync** trừ khi user yêu cầu.

### Ghi chú cho phiên sau
- `.jira-session.json` cũ (trước phiên này) sẽ KHÔNG có field `userKey` — sau khi user login lại 1 lần thì sẽ có. Nếu user phàn nàn ô User Key trống dù đã có session, kêu họ bấm "Đăng nhập & Lưu token" lại.
- Payload f-timesheet hiện vẫn dùng `fields: [{ id: 1, fieldName, fieldValue }]` — nếu field cần có nhiều dòng (id 1, 2, 3...) thì phải mở rộng UI thành mảng. Tạm thời chỉ 1 field.
- Đơn vị `remainingTime` trong payload gốc là `0` (Number). Đã giữ kiểu Number (giây) sau khi `*3600`. Nếu Jira plugin coi `remainingTime` là phút/giờ thì cần điều chỉnh hệ số — nên xác nhận với 1 issue test.

---

## Phiên 18:08

### Yêu cầu
User cung cấp 3 cURL để dò endpoint lấy danh sách option của field "Type Of Work" trên f-timesheet. Sau khi loại trừ, xác định được endpoint đúng là `GET /rest/f-timesheet/1.0/log-work/<issueKey>` (không phải `global-configuration/get-list` như mình đoán ban đầu — đó là plugin settings). Schema response: `[{ id, fieldName, fieldType, required, options: [{text, value}, ...] }]`. Gắn dropdown thay cho input free-text.

### Thay đổi
- `app.js:693-756` — thêm endpoint `POST /api/log-work-fields` (proxy `GET /rest/f-timesheet/1.0/log-work/<issueKey>`), trả `{ ok, fields: [...] }` đã trim đúng schema UI cần (id, fieldName, fieldType, required, options[{text,value}]).
- `app.js:165-186` — UI: section "Log Work Settings" có thêm input "Sample issue key" + nút "Tải options"; `#fieldName` và `#fieldValue` chuyển từ `<input>` text sang `<select>`. Mặc định ban đầu vẫn là `Type Of Work` / `Test` để không vỡ flow khi chưa load.
- `app.js:295-388` — thêm 3 hàm JS: `applyFieldConfig(fields)` populate fieldName select + `data-field-id`, `onFieldNameChange()` cascade options vào fieldValue, `loadFieldOptions()` gọi `/api/log-work-fields` và populate.
- `app.js:430-444` — `execute()` đọc `fieldName`/`fieldValue` từ `<select>` thay vì `<input>`; thêm `fieldId` lấy từ `data-field-id` của option đang chọn (fallback `1`). Gửi `fieldId` cùng `fieldName`/`fieldValue` lên backend.
- `app.js:786-790` — backend `/api/execute` đọc `fieldId` và truyền vào payload `fields: [{ id: fieldId, ... }]` thay cho hardcode `id: 1`.

### Quyết định & lý do
- **Endpoint đúng là `/rest/f-timesheet/1.0/log-work/<issueKey>`** chứ KHÔNG phải `/global-configuration/get-list`: cái sau là plugin settings (max giờ/ngày, description có required không...), không có options. Đã ghi note này để phiên sau không lặp lại điều tra.
- **Yêu cầu sample issue key thay vì auto-fetch**: endpoint cần issueKey. Có thể auto-pick từ JQL nhưng phải call `/issueNav/...` trước → thêm độ trễ và edge case (JQL chưa hợp lệ). Manual nhanh hơn, UX trade-off chấp nhận được vì options ít khi thay đổi → tải 1 lần là đủ cho cả batch.
- **Vẫn giữ `<select>` đơn (không multiple)**: response cho thấy `fieldType: "single-select-static"` → 1 giá trị. Code đã chừa nhánh `selectedOptions.map(...)` cho trường hợp tương lai có field multi-select.
- **Gửi `value` (vd `study-req`) thay vì `text` (`Study REQ`)**: khớp payload cũ (`fieldValue: ["Test"]` — `Test` là `value`).
- **`fieldId` truyền từ frontend** (không hardcode): response Jira show `id: 1` cho Type Of Work nhưng nếu plugin thêm field mới (id 2, 3...) thì code không vỡ.
- **Bỏ split-by-comma cho fieldValue** (đã có ở phiên 17:47): không cần nữa vì dropdown native xử lý. Code gọn hơn.

### Verify
- [x] `node --check app.js` pass.
- [x] `PORT=3099 node app.js` chạy; `GET /` có đủ `#sampleIssueKey`, `#loadFieldsBtn`, `<select id="fieldName">`, `<select id="fieldValue">`, JS `applyFieldConfig`/`onFieldNameChange`/`loadFieldOptions`.
- [ ] **Chưa verify trên Jira thật**: cần user login → nhập issue key (vd `VTSVOFFICE-15580`) → bấm "Tải options" → confirm dropdown hiện đủ ~26 option (Test, Study REQ, Q&A, ...). Sau đó chọn 1 option và execute 1 issue thử.

### Việc còn dang dở / cần làm tiếp
- (Tuỳ chọn) Auto-fill `sampleIssueKey` bằng issue đầu tiên từ JQL — phải thêm 1 server endpoint `/api/sample-issue` query `/issueNav/...` lấy 1 key. Cân nhắc khi user thấy bất tiện.
- (Tuỳ chọn) Hỗ trợ nhiều `fields` trong cùng 1 payload (nếu Jira plugin có thêm field config thứ 2) — hiện UI chỉ chọn 1 field tại 1 thời điểm.
- Phải confirm với user: khi `fieldType !== "single-select-static"` (vd "multi-select-static") thì có cần đổi `<select>` thành `multiple` không. Hiện chỉ thấy 1 type.

### Ghi chú cho phiên sau
- **CỰC KỲ QUAN TRỌNG (vì đã sai 1 lần phiên này)**: endpoint lấy field config là `GET /rest/f-timesheet/1.0/log-work/<issueKey>` (cùng URL với GET worklog list nhưng response shape là array field config, KHÔNG phải worklog entries). `GET /rest/f-timesheet/1.0/global-configuration/get-list` là plugin settings — đừng nhầm.
- Response schema option: `{ text: "Study REQ", value: "study-req" }` — gửi `value` cho Jira.
- Có vài option `value` chứa ký tự lạ (vd `quét-sonarq-&-attt` có dấu tiếng Việt + `&`) — đã encode bằng `JSON.stringify` body, không cần xử lý thêm.

---

## Phiên 18:30

### Yêu cầu
UX rework: (1) password có icon mắt toggle hiển thị, (2) ẩn toàn bộ phần dưới login cho đến khi đăng nhập xong, (3) đánh số bước 1/2/3/4 + khoanh đỏ để hướng dẫn user, (4) thêm nút "Quét" cho JQL trả về danh sách issue, (5) đổi Sample issue key thành dropdown + đưa lên đầu Log Work Settings, mặc định tải Field Name/Value từ issue đầu tiên, (6) Execute có 2 nút: "Sử dụng setting cho tất cả" và "Chỉ chạy cho <issueKey>".

### Thay đổi
- `app.js:914-959` — tách `runLogWork` cũ thành 2 helper: `fetchIssueKeys(JSESSIONID, XSRF, jql) -> Promise<string[]>` chỉ chạy JQL trả về key, và `logWorkForIssues(JSESSIONID, XSRF, issueKeys, config, send, done)` loop log work cho mảng key đã có.
- `app.js:867-895` — thêm endpoint `POST /api/scan-issues` (proxy `fetchIssueKeys`), trả `{ ok, issueKeys: [...] }`.
- `app.js:897-967` — `/api/execute` mở rộng: chấp nhận EITHER `jql` HOẶC `issueKeys[]`. Nếu có `issueKeys` thì bỏ qua scan, log work trực tiếp. Mode single-issue dùng `issueKeys: [key]`.
- `app.js:78-100` — thêm CSS: `.step` (border đỏ + radius), `.step-num` (badge tròn đỏ số 1/2/3/4), `.step-label` (chữ "Bước N — ..."), `.password-wrap` + `.eye-btn` cho icon mắt, `.hidden` class, `.exec-buttons` + `.btn-single` cho 2 nút execute (vàng cho "single").
- `app.js:108-228` — restructure HTML: 4 `<div class="step">` cho 4 bước; toàn bộ phần sau login wrap vào `<div id="mainContent" class="hidden">`. Login box bước 1; project+JQL+nút "Quét issue theo JQL" bước 2; Sample issue dropdown đặt lên đầu Bước 3 (Log Work Settings); 2 nút execute ở Bước 4.
- `app.js:241-258` — JS: thêm `setMainVisible(visible)`, `togglePassword()` (đổi type + icon 👁/🙈).
- `app.js:299` & `app.js:362` — gọi `setMainVisible(true)` sau khi `loadSavedSession` hoặc `login` thành công.
- `app.js:464-643` — JS rework execute:
  - `_scannedIssueKeys` cache list issue đã quét
  - `scanIssues()`: gọi `/api/scan-issues`, populate dropdown qua `populateSampleIssueDropdown`, auto gọi `loadFieldOptions()` cho issue đầu
  - `onSampleIssueChange()`: cập nhật label nút "Chỉ chạy cho <key>" + auto reload field options
  - `updateExecuteOneLabel()` / `updateExecuteButtonsState()`
  - `runExecute(issueKeys, _, contextLabel)` chung
  - `executeAll()` chạy `_scannedIssueKeys`; `executeOne()` chạy `[selectedKey]`

### Quyết định & lý do
- **Gửi `issueKeys[]` từ frontend thay vì JQL khi đã quét xong**: tránh re-scan, đảm bảo execute đúng danh sách user đã thấy/chọn (nếu Jira data thay đổi giữa lúc quét và execute thì cũng không bị drift).
- **"Quét" và "Tải options" giữ riêng**: scan auto-trigger load nhưng vẫn để nút "Tải lại options" thủ công vì khi user đổi sample issue cũng cần load lại (nếu khác project) — `onSampleIssueChange` đã làm tự động, nhưng nút manual là escape hatch nếu API lỗi.
- **2 nút Execute màu khác nhau**: nút "tất cả" giữ xanh primary (action chính), nút "1 issue" màu vàng (warning màu nhạt) để phân biệt rõ — tránh user bấm nhầm khi định chạy single nhưng lại click All.
- **Hide với `.hidden` class thay vì xoá DOM**: tốc độ + giữ trạng thái form khi user logout/login lại trong cùng session.
- **Step marker dùng border đỏ + badge tròn đỏ**: theo yêu cầu "khoanh dấu đỏ và đánh số". Badge có ring `box-shadow: 0 0 0 3px #1e1e2e` tạo hiệu ứng "tem" nổi trên border.
- **Icon mắt dùng emoji 👁 / 🙈**: không cần asset, render được trên Windows. Trade-off: kích thước hơi nhỏ; nếu cần SVG sau này dễ thay.
- **Validate ở `runExecute` trước khi mở stream**: tránh trường hợp đã `writeHead` rồi mới phát hiện thiếu param → khó trả về JSON error đúng kiểu.

### Verify
- [x] `node --check app.js` pass.
- [x] Server chạy `:3005`, `GET /` HTTP 200, HTML có đủ: `#mainContent.hidden`, 4 `step-num` (1/2/3/4), `Bước 1–4`, `#eyeBtn` + `togglePassword`, `#scanBtn` + `scanIssues`, `#sampleIssueKey` select, `#executeAllBtn` / `#executeOneBtn`.
- [ ] **Chưa verify trên Jira thật**: cần test live full flow — login → mainContent xuất hiện → chọn project → bấm "Quét" → dropdown sample issue có data → field options auto load → đổi sample issue → options reload → bấm "Chỉ chạy cho VTSVOFFICE-XXX" thử log 1 issue.

### Việc còn dang dở / cần làm tiếp
- Khi password input có icon mắt, browser autocomplete có thể che lên nút. Nếu user phàn nàn → cần test trên Edge/Chrome thật.
- Nếu user logout (xoá session) cần button "Đăng xuất" để gọi `setMainVisible(false)` — hiện không có (CLAUDE.md không yêu cầu).
- Khi scan trả về 0 issue, dropdown vẫn show "-- Không có issue --" nhưng 2 nút execute disabled — UX OK; nếu cần message rõ hơn thì thêm hint.

### Ghi chú cho phiên sau
- Server hiện đang chạy ở task background `bd9cnr2vu` (cổng 3005). Nếu cần restart, stop task này trước rồi `node app.js`.
- `/api/execute` nay chấp nhận cả `jql` lẫn `issueKeys[]` — frontend mặc định gửi `issueKeys`. Nếu cần fallback (vd không scan trước) vẫn gửi `jql` được, backend sẽ tự scan rồi log.
- `runLogWork` cũ đã bị xoá; mọi caller phải dùng `fetchIssueKeys` + `logWorkForIssues` riêng biệt. File `update Logwork Jira.js` (phiên bản cũ) KHÔNG được sync — vẫn còn `runLogWork` cũ, theo CLAUDE.md là chủ ý.

---

## Phiên 19:15

### Yêu cầu
Progressive disclosure: trước khi login xong thì các Bước 2/3/4 phải đóng (collapse), hoàn thành bước nào mới mở bước kế tiếp.

### Thay đổi
- `app.js:104,126,175` — gán `id="step2"`, `id="step3"`, `id="step4"` cho 3 `.step` div tương ứng; step3 và step4 thêm class `hidden` mặc định.
- `app.js:260-271` — thêm 2 helper JS: `showStep(n)` remove `.hidden` cho `#stepN`, `hideStepsFrom(n)` loop ẩn từ bước n trở đi (dùng khi quét 0 issue → đóng lại Bước 3/4).
- `app.js:515-523` — trong `scanIssues()`: khi quét OK có ≥1 issue → `showStep(3)` rồi await `loadFieldOptions()` rồi `showStep(4)`. Nếu 0 issue → `hideStepsFrom(3)` (giữ user ở Bước 2 để sửa JQL).

### Quyết định & lý do
- **Bước 2 KHÔNG cần `hidden` class riêng**: nó nằm trong `#mainContent` vốn đã ẩn cho đến khi login → đủ.
- **Bước 4 mở sau khi `loadFieldOptions()` await xong (cả thành công lẫn fail)**: nếu API options lỗi, user vẫn có thể chạy execute với default field — không chặn flow. Cân nhắc strict hơn (chỉ mở khi load OK) nhưng UX cứng.
- **Reset Bước 3/4 khi quét rỗng** (`hideStepsFrom(3)`): tránh user thấy form Settings rồi quét lại JQL khác trả 0 → form vẫn show với data cũ gây nhầm.
- **KHÔNG đóng lại Bước 3 khi user đổi sample issue / load lại options**: progressive 1 chiều — đã mở rồi thì không đóng (trừ trường hợp quét rỗng).

### Verify
- [x] `node --check` pass, server restart OK (task `b0x3h8dh0`), `GET /` HTTP 200.
- [x] HTML có `id="step2"` (không hidden), `id="step3"` + `id="step4"` (có hidden class).
- [ ] **Chưa test live**: cần user reload browser (Ctrl+F5) → confirm: lúc đầu chỉ thấy login → login xong thấy Bước 2 → quét xong thấy Bước 3 và Bước 4.

### Việc còn dang dở / cần làm tiếp
- Khi user logout (chưa implement) chưa reset step3/step4 về hidden. Nếu sau này thêm logout cần gọi `hideStepsFrom(3)` (và đóng mainContent).

### Ghi chú cho phiên sau
- Server cũ task `bd9cnr2vu` đã stop. Server mới task `b0x3h8dh0` ở port 3005.

---

## Phiên 19:44

### Yêu cầu
Token (JSESSIONID, XSRF) đang được lưu trong `.jira-session.json` (server-side). User muốn: tắt browser thì clear hết — tức là không persist xuống đĩa, chỉ giữ trong scope của tab/browser.

### Thay đổi
- `app.js:1-7` — bỏ `require("fs")` và `require("path")` (không còn dùng); giữ `https`, `http`, `querystring`, `dns`.
- `app.js:30` — xoá const `SESSION_FILE`.
- `app.js:856-859` — xoá 2 hàm `saveSession`/`loadSession`; thay bằng comment ngắn giải thích token nay nằm trong sessionStorage.
- `app.js:868` (xoá ~18 dòng) — xoá endpoint `GET /api/session`. Browser không gọi server để lấy session nữa.
- `app.js:910` — `/api/login` không gọi `saveSession()`; chỉ trả token cho frontend.
- `app.js:290-321` (HTML inline) — thêm 3 helper JS: `saveSessionToStorage(s)`, `readSessionFromStorage()`, `clearSessionStorage()` dùng `sessionStorage` với key `jira_session_v1`. Rewrite `loadSavedSession()` từ async fetch → sync read sessionStorage.
- `app.js:362-368` — sau khi login OK, gọi `saveSessionToStorage(...)` để autofill trong cùng tab khi reload (F5). Sửa hint text: "Token chỉ tồn tại trong tab này. Đóng tab/browser sẽ clear."
- File đĩa `.jira-session.json` — đã `rm` để xoá token cũ còn sót.

### Quyết định & lý do
- **Dùng `sessionStorage` (KHÔNG `localStorage`)**: `sessionStorage` clear khi đóng tab/browser, đúng yêu cầu. `localStorage` persist vĩnh viễn, không match. `IndexedDB` quá nặng cho 1 token nhỏ.
- **Bỏ hoàn toàn server-side persist**: vì sessionStorage đã đủ — không cần redundant copy ở server (lại còn dễ leak nếu user quên xoá file). Sạch hơn.
- **Vẫn lưu khi login OK (không phải "lưu sau mỗi request")**: token không đổi giữa các request trong cùng phiên Jira, nên 1 lần ghi sau login là đủ. Nếu Jira invalidate session, user phải login lại — sessionStorage sẽ được overwrite tại đó.
- **Hằng số `SESSION_KEY = 'jira_session_v1'`**: version suffix để sau này nếu shape đổi (vd thêm field), có thể bump v2 và bỏ qua data cũ thay vì crash khi parse.
- **Giữ ô input JSESSIONID/XSRF_TOKEN trong UI** (vẫn ở `<details>`): user vẫn dán tay được nếu cần, không bị block.
- **KHÔNG implement nút Logout**: user không yêu cầu, đã có `clearSessionStorage()` sẵn cho phiên sau gắn nếu cần.

### Verify
- [x] `node --check` pass; server restart task `boak47if2`.
- [x] `GET /` HTTP 200; `GET /api/session` HTTP 404 (đã bỏ).
- [x] File `.jira-session.json` không còn (`ls` báo "No such file or directory").
- [x] HTML có code `sessionStorage.setItem/getItem`, không còn fetch `/api/session`.
- [ ] **Chưa test live full flow**: cần user reload tab → confirm "Chưa đăng nhập" (vì sessionStorage rỗng), đăng nhập → F5 trong tab đó → vẫn ở trạng thái logged-in (đọc lại sessionStorage), đóng tab/browser → mở lại → phải login lại.

### Việc còn dang dở / cần làm tiếp
- (Optional) Thêm nút "Đăng xuất" gọi `clearSessionStorage()` + reload — nếu user yêu cầu sau này.
- CLAUDE.md mục 2 và 4 vẫn nhắc `.jira-session.json` như source-of-truth của token. Cần update lại đoạn đó (token nay ở sessionStorage browser, không còn ở đĩa). KHÔNG tự sửa CLAUDE.md trừ khi user xác nhận — đó là tài liệu owner.

### Ghi chú cho phiên sau
- `.jira-session.json` đã không còn được app dùng. Nếu thấy file này tái xuất hiện, là có ai đó vô tình restore — không phải app tạo lại.
- Server task hiện tại: `boak47if2` (port 3005). Server cũ `b0x3h8dh0` đã stop.
- Toàn bộ `fs`/`path` import đã bị xoá. Nếu phiên sau cần đọc/ghi đĩa lại (vd export báo cáo, log file), phải `require` lại 2 module này.

---

## Phiên 19:54

### Yêu cầu
Cập nhật tài liệu + đánh version cho sản phẩm. User chốt: **v1.0.0** (first stable), **tạo CHANGELOG.md riêng** theo Keep-a-Changelog, **hiển thị badge version cạnh tiêu đề UI**.

### Thay đổi
- `app.js:5-8` — thêm const `VERSION = "1.0.0"` ngay sau các import. Comment: "SemVer. Bump theo CHANGELOG.md mỗi khi release."
- `app.js:53-54` — CSS `.version-badge` (chip xám tròn) và `h1 { display: flex; gap: 12px }` để badge nằm inline với tiêu đề.
- `app.js:107` — `<h1>Jira Log Work Tool <span class="version-badge">v${VERSION}</span></h1>` — inject VERSION qua template literal (HTML là `${...}` string).
- `CHANGELOG.md` (mới) — Keep-a-Changelog format. Section `[1.0.0] — 2026-05-28` liệt kê: Added (login + autofetch userKey, scan-issues, log-work-fields, settings form, dropdown Field options, 2 nút execute, progressive disclosure, step markers, eye toggle, custom DNS, version badge), Security (token sessionStorage, password không persist), Removed (saveSession/loadSession/api/session, fs/path imports). Cuối file có section "Versioning policy" giải thích MAJOR/MINOR/PATCH.
- `CLAUDE.md:8-15` (mục 1) — thêm dòng version + đổi stack list (bỏ `fs`).
- `CLAUDE.md:16-26` (mục 2) — cập nhật cấu trúc thư mục: bỏ `.jira-session.json`, thêm `CHANGELOG.md`; thêm ghi chú "Nếu thấy `.jira-session.json` tái xuất hiện, không phải app tạo".
- `CLAUDE.md:31-50` (mục 3) — kiến trúc cập nhật: liệt kê 6 endpoint hiện tại (bỏ `/api/session`, thêm `/api/scan-issues`, `/api/log-work-fields`), nói rõ `fetchIssueKeys` + `logWorkForIssues` thay cho `runLogWork` cũ, đề cập progressive disclosure 4 bước. Đổi mục "Hằng số mặc định": dùng `DEFAULT_*` mới + thêm `VERSION`.
- `CLAUDE.md:50-58` (mục 4) — đổi quy ước "Không commit `.jira-session.json`" → "Không persist token xuống đĩa" (chỉ RAM + sessionStorage). Thêm quy ước "Khi bump version → sửa app.js + CHANGELOG.md".
- `CLAUDE.md:60-65` (mục 5) — đổi mô tả token (sessionStorage, không file), nêu rõ password chỉ sống trong memory 1 request `/api/login`.
- `CLAUDE.md:104-127` (mục 9 mới) — section Versioning: SemVer rules + quy trình bump 4 bước (sửa VERSION, thêm section CHANGELOG, ghi report, không bump bừa).

### Quyết định & lý do
- **v1.0.0 thay vì v0.x.0**: tool đã có flow hoàn chỉnh, test được end-to-end, không còn major work-in-progress → coi như stable. v0.x dành cho khi user định trade-off API tự do.
- **CHANGELOG.md riêng (không nhúng vào CLAUDE.md)**: theo Keep-a-Changelog convention. Tách bạch: `reports/` per-session log, `CHANGELOG.md` version-level user-facing, `CLAUDE.md` context cho AI agent.
- **Badge UI dùng `${VERSION}` template literal**: HTML inline trong app.js là JS template string → có thể inject thẳng. Không cần build/replace tool.
- **Style badge nhẹ (xám trên xanh)**: không nổi bật quá, chỉ làm chip metadata. Người dùng thấy được nhưng không phân tâm khỏi flow chính.
- **Mục 9 viết quy trình bump rõ ràng**: tránh phiên sau bump bừa hoặc quên cập nhật CHANGELOG.

### Verify
- [x] `node --check app.js` pass.
- [x] Server restart task `bqmbtvgsx`, `GET /` HTTP 200.
- [x] HTML render `<span class="version-badge">v1.0.0</span>` cạnh `<h1>`.
- [x] CHANGELOG.md tồn tại với section `[1.0.0] — 2026-05-28`.
- [x] CLAUDE.md đã update: mục 1 có version, mục 2 không còn `.jira-session.json`, mục 3 list endpoint mới, mục 9 mới về versioning.
- [ ] Chưa test live UI render badge trên browser (cần user reload Ctrl+F5).

### Việc còn dang dở / cần làm tiếp
- Khi release v1.0.1 / v1.1.0 / v2.0.0 sau này: theo quy trình mục 9 CLAUDE.md.
- Có thể thêm `.gitignore` (chưa có) để chính thức loại trừ `.jira-session.json`, `node_modules/`, `*.log` — nhưng không có dependencies và file token cũng không còn được tạo nên ưu tiên thấp.
- (Optional) Thêm tooltip lên badge hiển thị changelog summary — chưa cần.

### Ghi chú cho phiên sau
- **Bump version**: sửa `VERSION` ở [app.js:8](app.js), thêm section đầu [CHANGELOG.md](CHANGELOG.md). Đừng quên update UI sẽ tự reflect qua template literal — chỉ cần restart server.
- Server task hiện tại: `bqmbtvgsx` (port 3005). Server cũ `boak47if2` đã stop.
- File `update Logwork Jira.js` (legacy) vẫn chưa được sync — chủ ý theo CLAUDE.md mục 2.
